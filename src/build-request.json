{
  "kind": "build_request",
  "title": "Add gentle fade-in animation to the romantic hero image",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-4",
      "text": "Add a gentle fade-in animation for the romantic hero image on the main (pre-Yes) screen so the image smoothly appears on initial render.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-5"
        ],
        "quotes": [
          "Ok add the animation"
        ]
      },
      "acceptanceCriteria": [
        "When the app first loads on the main prompt screen, the hero image fades in smoothly (not an abrupt appearance).",
        "The fade-in is subtle and does not distract from the existing layout or interactions.",
        "The existing floating motion on the hero image remains (fade-in should not remove the current float effect).",
        "If the user has reduced-motion enabled, the fade-in animation is disabled or minimized."
      ]
    }
  ],
  "constraints": [
    "Do not modify files in frontend immutablePaths (frontend/src/hooks/useInternetIdentity.ts, frontend/src/hooks/useInternetIdentity.tsx, frontend/src/hooks/useActor.ts, frontend/src/main.tsx, frontend/src/components/ui).",
    "Keep backend as-is (single-actor placeholder); no backend methods or state are required for this change."
  ],
  "nonGoals": [
    "Do not change the Valentine Yes/No flow behavior.",
    "Do not introduce new backend queries, persistence, or Internet Identity-related changes.",
    "Do not change or regenerate the existing hero image asset."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}